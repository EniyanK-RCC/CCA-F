Start the local development server.

The user provided this argument: $ARGUMENTS

- If $ARGUMENTS is "setup" or "first-time": run `npm run setup` first (installs deps, generates Prisma, runs migrations), then run `npm run dev`
- Otherwise: run `npm run dev` only

Run the server in the background so it stays alive. The server will be available at http://localhost:3000 (or the next available port if 3000 is in use).
